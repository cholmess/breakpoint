# Test fixtures

- **failure-events.json**: Mock failure events for Person B statistics/analysis tests. Config IDs `"A"` and `"B"` are intentional mocks for isolated unit tests; they do not match production config IDs (`config-a`, `config-b`, etc.).
